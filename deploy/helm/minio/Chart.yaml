apiVersion: v2
name: minio-observability-storage
description: MinIO object storage for observability data (Tempo traces, Loki logs)
type: application
version: 0.1.0
appVersion: "latest"

# No external dependencies - using our own templates with configurable namespace
# Set targetNamespace in values.yaml to deploy to a specific namespace

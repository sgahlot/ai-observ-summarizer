---
# Operator: loki-operator
# Full Name: loki-operator.openshift-operators-redhat
# CRD Patterns: loki.grafana.com
# Note: Uninstalling this operator will delete all CRDs matching the above patterns
---
apiVersion: v1
kind: Namespace
metadata:
  name: ${NAMESPACE}
---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  generateName: ${NAMESPACE}-
  namespace: ${NAMESPACE}
spec: {}
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: loki-operator
  namespace: ${NAMESPACE}
spec:
  channel: ${CHANNEL}
  name: loki-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  installPlanApproval: Manual
  startingCSV: loki-operator.v6.3.4
